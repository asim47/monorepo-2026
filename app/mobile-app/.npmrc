# React Native specific configuration
# Install dependencies locally to avoid version conflicts from hoisting
# This ensures mobile-app gets exact versions without affecting other workspaces
install-strategy=shallow
legacy-peer-deps=true
