# Workspace configuration
# Allow workspaces to have their own versions of dependencies
legacy-peer-deps=false

# This helps with React Native in monorepos
# Allows different React versions in different workspaces
